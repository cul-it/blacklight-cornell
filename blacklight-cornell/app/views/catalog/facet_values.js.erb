const facetDiv = document.querySelector('#facet-lc_callnum_facet div.card-body');
if (facetDiv) {
  facetDiv.innerHTML = "<%= j render Blacklight::FacetComponent.new(display_facet: @display_facet,
                                                                    blacklight_config: blacklight_config,
                                                                    layout: false) %>";
}

// Re-initialize blacklight-hierarchy behavior to display +/- icons correctly
Blacklight.do_hierarchical_facet_expand_contract_behavior();
