<%# TODO: Fix spacing when no constraints, e.g. advanced search form %>
<%# Can remove this partial in bl8 %>
<%= render((blacklight_config&.view_config(document_index_view_type)&.constraints_component || ConstraintsComponent).new(search_state: convert_to_search_state(controller.params != params ? params : search_state))) %>
