- if @counter.blank?
  = link_to catalog_path(@id), { :title => @ti } do
    %i.fa.fa-arrow-circle-left
    Back to item
- else
  = link_to catalog_path(@id), { :title => @ti, 'data-counter'.to_sym => @counter } do
    %i.fa.fa-arrow-circle-left
    Back to item
%h1.request-type Ask a Librarian
%p 
  We are currently unable to place a request for this item. Please 
  =link_to 'Ask a Librarian','http://ask.library.cornell.edu'
  for more help.
