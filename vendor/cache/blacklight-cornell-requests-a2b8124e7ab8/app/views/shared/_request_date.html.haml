-d30 = 30.day.from_now
-d60 = 60.day.from_now
-d180 = 180.day.from_now
.form-group
  %label.control-label.col-sm-2{:for => 'latest-date'} If item remains unavailable, cancel request after:
  .col-sm-10#latest-date
    .radio
      %label{ :for => "days_30"}
        %input(type="radio" name="latest-date" value="#{d30.strftime("%Y-%m-%d")}" id="days_30")
        30 days (#{d30.strftime("%m-%d-%Y")})
    .radio
      %label{ :for => "days_60"}
        %input(type="radio" name="latest-date" value="#{d60.strftime("%Y-%m-%d")}" id="days_60")
        60 days (#{d60.strftime("%m-%d-%Y")})
    .radio
      %label{ :for => "days_180"}
        %input(type="radio" name="latest-date" value="#{d180.strftime("%Y-%m-%d")}" id="days_180" checked="checked")
        180 days (#{d180.strftime("%m-%d-%Y")})