module BlacklightCornellRequests
  # Seems that we're using month/year for the version number for now.
  # Guess once we hit the end of the year, we might want to switch
  # to something a little more traditional?
  VERSION = "1.0.1"
end
