//  main container for facets/limits menu 
// jac244 Delete original lines 11 and 12 to remove 'more' link in Call Number facet display
// Original lines 12 and 13 follow
//      - when "lc_1letter_facet"
//        = fix_catalog_links(render(:partial => "catalog/facet_call_letter", :locals => {:solr_field => solr_fname}))


%p{:class => "hidden-phone"} Refine your results
%div{:class => "navbar"}
  %a{:class => "btn btn-navbar" , "data-toggle" => "collapse", "data-target" => ".nav-collapse"}Refine results
  %div{:class => "nav-collapse collapse"}
    - facet_field_names.reject { |name| facet = @response.facet_by_field_name(name); facet.nil? || facet.items.empty? }.each do |solr_fname|
      -
      %div{:class => "blacklight-#{solr_fname.parameterize}"}
        - case solr_fname
        - when "format"
          = fix_catalog_links(render_facet_partials([solr_fname], :partial => "facet_limit_selected"))
        - when "lc_2letter_facet", "title_first_facet"
        - else
          = fix_catalog_links(render_facet_partials([solr_fname]))
