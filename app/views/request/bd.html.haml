= link_to catalog_path(@id), {:title => @ti} do
  %i.icon-circle-arrow-left
  Back to item
%h1.request-type Borrow Direct Request
%h2.item-title-request.blacklight-title_display=@ti
%div.request-author=@au
%p
  Click the button below to locate your item in the  Borrow Direct catalog and place a request.
%p
  Approximate delivery time is
  = @delivery_time
  days.
- if @isbn.present?
  = link_to 'Search Borrow Direct', borrowdirect_url_from_isbn(@isbn), :class => 'btn btn-danger'
- else
  = link_to 'Search Borrow Direct', borrowdirect_url_from_title(@ti), :class => 'btn btn-danger'
.controls
  %div.accordion#accordion2.request-options.form-alternative-action
    %div.accordion-group
      %div.accordion-heading
      =link_to 'Other options', '#collapseOne', {:data => {:toggle => 'collapse', :parent => '#accordion2'}}
      %div#collapseOne.accordion-body.collapse
        %div.accordion-inner
          %div.span4
            = render :partial => 'shared/request_options'
