%h1.request-type Borrow Direct Request
%h2.item-title-request.blacklight-title_display=@ti
%div.request-author=@au
%p
  To request this item through Borrow Direct, please use the following link. Approximate delivery time is 
  = @delivery_time
  days.
- if @isbn.present?
  = link_to 'Search Borrow Direct', borrowdirect_url_from_isbn(@isbn)
- else
  = link_to 'Search Borrow Direct', borrowdirect_url_from_title(@ti)


.controls
  %div.accordion#accordion2.request-options
    %div.accordion-group
      %div.accordion-heading
      =link_to 'Other options', '#collapseOne', {:data => {:toggle => 'collapse', :parent => '#accordion2'}} 
      %div#collapseOne.accordion-body.collapse
        %div.accordion-inner     
          %div.span4     
            = render :partial => 'shared/request_options'

