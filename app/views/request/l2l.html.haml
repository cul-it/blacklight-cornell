%p
  Request this item be delivered for you: 
  %br
  %b=@ti
  %form{:id => 'req', :method => 'post', :action => "#{@voyager_request_handler_url}/holdings/callslip/#{@netid}/#{@id}"}
    %input(type="hidden" name="bid" value="#{@id}")
    %br
    = render :partial => 'shared/reqpu'
    %br
    = render :partial => 'shared/reqco'
    %br
    Comments:
    %br
    %textarea
    %br
    = render :partial => 'shared/reqdate'
    %br
    %input(type="submit" id="request_sumbit" value="Submit request for delivery")
    %input(type="reset" id="reest" value="Clear form")
    %br
    %div(id="result" name="result" value="")
%p Find me in app/views/request/l2l.html.haml
